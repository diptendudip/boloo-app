# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9
geoalchemy2==0.14.3

# Data Validation
pydantic==2.5.0
pydantic-settings==2.1.0
jsonschema==4.20.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
slowapi>=0.1.9

# Azure Services
azure-cognitiveservices-speech==1.34.0
openai==1.3.0

# AI/LLM (Claude API via Anthropic)
anthropic==0.9.0

# HTTP & Async
httpx==0.25.1
aiofiles==23.2.1
requests==2.31.0

# Phone Number Validation
phonenumbers==8.13.30

# Audio Processing
pydub==0.25.1

# Email (if needed)
aiosmtplib==3.0.1

# Environment Variables
python-dotenv==1.0.0

# Redis (optional - for caching)
redis==5.0.1

# NumPy (for data processing)
numpy==1.26.2

# String matching (for fuzzy text comparison)
rapidfuzz==3.5.2

# Storage (MinIO for S3-compatible object storage)
minio==7.2.0

# Monitoring & Telemetry (OPTIONAL - for Azure Application Insights integration)
# Uncomment if you want to enable Application Insights telemetry:
# opencensus-ext-azure==1.1.13
# opencensus-ext-logging==0.1.1
# opencensus-ext-requests==0.8.0

# Vector Search (DISABLED - Requires 1.2GB+ RAM, use only on B2+ tier)
# Uncomment these if ENABLE_VECTOR_SEARCH=1 and you have sufficient memory:
# faiss-cpu==1.7.4
# sentence-transformers==2.2.2
